# Copy to .env.local and paste your key after the '='.
# Get one at https://platform.claude.com — create a workspace with a spend limit
# and scope the key to it, so this project can never exceed its cap.
#
# NEVER commit the real value. .env.local is gitignored.
ANTHROPIC_API_KEY=
